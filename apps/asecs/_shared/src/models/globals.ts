import { GlobalConfig } from 'payload';

export const globals: GlobalConfig[] = [
  {
    slug: 'site_settings',
    fields: [
      {
        name: 'title',
        type: 'text'
      }
    ]
  }
];
