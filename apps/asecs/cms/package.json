{
  "name": "payload-cms",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "next build",
    "build:api": "tsc",
    "mongo:shell": "docker run -it mongodb/mongodb-community-server:$MONGODB_VERSION mongosh \"mongodb://user:pass@clusterURL/database\"",
    "mongo:check": "tsx check-mongodb-connection.ts",
    "docker:down": "docker-compose down",
    "docker:up": "mkdir -p .mongodb-data && docker-compose up -d",
    "pipeline:deploy:dev": "APP_ENV=dev npm run build && NODE_ENV=dev cdk deploy --all --force --require-approval never",
    "pipeline:deploy:stage": "APP_ENV=stage npm run build && NODE_ENV=stage cdk deploy --all --force --require-approval never",
    "pipeline:deploy:prod": "APP_ENV=prod npm run build && NODE_ENV=prod cdk deploy --all --force --require-approval never",
    "deploy:dev": "APP_ENV=dev npm run build && NODE_ENV=dev cdk deploy --all --force --require-approval never --profile asecs-dev",
    "deploy:stage": "APP_ENV=stage npm run build && NODE_ENV=stage cdk deploy --all --force --require-approval never --profile asecs-stage",
    "deploy:prod:force": "APP_ENV=prod npm run build && NODE_ENV=prod cdk deploy --all --force --require-approval never --profile asecs-prod",
    "start": "cross-env NODE_OPTIONS=--no-deprecation next dev",
    "start:prod": "cross-env NODE_OPTIONS=--no-deprecation next start"
  },
  "dependencies": {
    "@payloadcms/db-mongodb": "3.61.1",
    "@payloadcms/email-nodemailer": "3.61.1",
    "@payloadcms/next": "3.61.1",
    "@payloadcms/richtext-lexical": "3.61.1",
    "@payloadcms/translations": "3.61.1",
    "@payloadcms/ui": "3.61.1",
    "next": "15.5.6",
    "payload": "3.61.1",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "sharp": "0.34.4"
  },
  "devDependencies": {
    "@aws-sdk/client-ec2": "3.919.0",
    "@aws-sdk/credential-providers": "3.919.0",
    "@aws-sdk/shared-ini-file-loader": "3.374.0",
    "@agapi-development-tools/nextjs-lambda": "workspace:*",
    "@next/eslint-plugin-next": "15.5.6",
    "@types/node": "22.12.0",
    "@types/react": "19.2.2",
    "@types/react-dom": "19.2.2",
    "aws-cdk": "2.1030.0",
    "cross-env": "10.1.0",
    "dotenv": "17.2.3",
    "glob": "10.4.1",
    "mongodb": "6.20.0",
    "nodemon": "3.1.10",
    "typescript": "5.9.3",
    "ts-node": "10.9.2",
    "tsx": "4.20.6"
  }
}
